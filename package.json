{
    "name": "hyndriuk-web",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "sync:pages": "node tools/sync-pages.mjs",
        "dev": "npm run sync:pages && vite",
        "build": "npm run sync:pages && vite build",
        "preview": "vite preview",
        "test": "playwright test",
        "lint": "eslint .",
        "format": "prettier --write .",
        "format:check": "prettier --check .",
        "check": "npm run lint && npm run format:check && npm run build && npm run test"
    },
    "devDependencies": {
        "@eslint/js": "^10.0.1",
        "@playwright/test": "^1.59.1",
        "eslint": "^10.2.1",
        "eslint-config-prettier": "^10.1.8",
        "globals": "^17.5.0",
        "prettier": "^3.8.3",
        "vite": "^7.0.0"
    }
}
